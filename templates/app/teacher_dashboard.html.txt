<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* Sidebar */
        .sidebar {
            background-color: #2c3e50;
            color: #ecf0f1;
            width: 250px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 20px 0;
        }

        .sidebar h4 {
            text-align: center;
            margin-bottom: 30px;
        }

        .sidebar nav a {
            display: block;
            text-decoration: none;
            color: #ecf0f1;
            padding: 10px 20px;
            margin: 10px 0;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .sidebar nav a:hover,
        .sidebar nav a.active {
            background-color: #34495e;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 20px;
            background-color: #f8f9fa;
        }

        .top-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .profile img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-right: 15px;
            border: 2px solid #34495e;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 col-lg-2 sidebar">
                <h4>KINDY ADMIN PANEL</h4>
                <nav>
                    <a href="?section=dashboard" class="{% if section == 'dashboard' %}active{% endif %}">Dashboard</a>
                    <a href="?section=search_toddler" class="{% if section == 'search_toddler' %}active{% endif %}">Search Toddler</a>
                    <a href="?section=add_toddler" class="{% if section == 'add_toddler' %}active{% endif %}">Add Toddler</a>
                    <a href="?section=toddler_payment" class="{% if section == 'toddler_payment' %}active{% endif %}">Toddler Payment</a>
                    <a href="?section=feedback" class="{% if section == 'feedback' %}active{% endif %}">Feedback</a>
                </nav>
            </div>

            <!-- Main Content -->
            <div class="col-md-9 col-lg-10 main-content">
                <div class="top-section">
                    <h3>WELCOME Ms. {{ user.username }}</h3>
                    <div class="profile">
                        <img src="https://via.placeholder.com/50" alt="Profile Picture">
                        <span>Kindy Teacher</span>
                    </div>
                </div>

                <!-- Section-Based Content -->
                {% if section == 'dashboard' %}
                <div>
                    <h2>Dashboard</h2>
                    <p>Welcome to your dashboard. Great Work!</p>
                </div>
                {% elif section == 'search_toddler' %}
                <div>
                    <h2>Search Toddler</h2>
                    <p>Search for toddlers here.</p>
                </div>
                {% elif section == 'add_toddler' %}
                <div>
                    <h2>Add Toddler</h2>
                    <p>Use this section to add a new toddler.</p>
                </div>
                {% elif section == 'toddler_payment' %}
                <div>
                    <h2>Toddler Payment</h2>
                    <p>Manage payments here.</p>
                </div>
                {% elif section == 'feedback' %}
                <div>
                    <h2>Feedback</h2>
                    <p>View feedback here.</p>
                </div>
                {% else %}
                <div>
                    <h2>Welcome</h2>
                    <p>Select a section to get started.</p>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
